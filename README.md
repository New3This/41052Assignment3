Inmstructions: Each algorithm is in separate branches. The randomised algorithm is in the branch, Gordon while the deterministic algorithm is in the branch, Dylan. 

The project can be opened with visual studio community 2022 via the .sln file. The C++ standard used has to be at C++17 or newer in order for gtest to function.

The terminal is not used to run the code, rather the IDE's run button is used.
